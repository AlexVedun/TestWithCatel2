<catel:UserControl x:Class="TestWithCatel.Views.SliderResultsView"
                   xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                   xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                   xmlns:catel="http://schemas.catelproject.com"
                   Background="{DynamicResource {x:Static SystemColors.WindowBrushKey}}"
                   VerticalAlignment="Stretch">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0">
            <Label  Content="Количество правильных ответов:" FontFamily="Arial" FontSize="16" Margin="0, 0, 5, 0"/>
            <Label Content="{Binding Score}" FontFamily="Arial" FontSize="16"/>
        </StackPanel>
    </Grid>

</catel:UserControl>
